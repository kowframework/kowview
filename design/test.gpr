with "aws.gpr";
with "awconfig-xml.gpr";

project Test is

	for Source_Dirs use ("src");
	for Exec_Dir use "bin";
	for Object_Dir use "obj";


	for Main use ("server");
	
	package Linker is
		for Default_Switches ("Ada") use ("-lcrypto", "-lssl");
	end Linker;

end Test;
